{
  "name": "my-bank-js",
  "version": "0.1.0",
  "private": true,
  "description": "My Bank — monorepo Node.js + Angular",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:backend": "npm test -w backend"
  },
  "engines": {
    "node": ">=18"
  }
}
